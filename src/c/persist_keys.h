#pragma once

// Centralized persistent storage keys used across the app.
typedef enum PersistKey {
    PERSIST_KEY_SETTINGS = 1,
    PERSIST_KEY_SETTINGS_VERSION = 2,
    PERSIST_KEY_WEATHER_DATA = 3,
} PersistKey;

